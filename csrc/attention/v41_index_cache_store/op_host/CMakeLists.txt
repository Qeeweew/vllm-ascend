# SPDX-License-Identifier: Apache-2.0
add_op_to_compiled_list()
if (BUILD_OPEN_PROJECT)
    set(v41_index_cache_store_depends attention/v41_rope CACHE INTERNAL "V41 rotary device core")
    target_sources(op_host_aclnn PRIVATE v41_index_cache_store_def.cpp)
endif()
add_ops_compile_options(OP_NAME V41IndexCacheStore OPTIONS --cce-auto-sync=off -Wno-deprecated-declarations)
if (NOT BUILD_OPS_RTY_KERNEL)
    add_modules_sources(OPTYPE v41_index_cache_store ACLNNTYPE aclnn)
    target_include_directories(${OPHOST_NAME}_tiling_obj PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
endif()
